# Nhật ký quyết định

> Agent ghi qua skill **decision-log**. Mới nhất trên cùng. Quyết định đảo
> ngược thì thêm dòng mới, không xoá dòng cũ.

| Quyết định                                                                                                              | Vì sao                                                                                                                                                                                                                                                                                                                                                                                                           | Phương án bị loại                                                                                                                                                                                                                                                      | Phạm vi                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T3/devbox = môi trường dev chính thức; T3 host cũ chỉ logout credential, KHÔNG chạy server trên host nữa                | Server T3 duy nhất cần là trong devbox (compose `t3-code`, qua tunnel + relay); T3 host cũ từng bind 3773 thất bại âm thầm (docker-proxy giữ port) gây nghi sai chẩn đoán; giữ binary host chỉ để `t3 uninstall` tuỳ chọn sau                                                                                                                                                                                    | Gỡ T3 host ngay + giữ làm dự phòng (2 nơi = 2 tài khoản = rối account, đã cắn 1 lần); cài lại T3 host song song devbox                                                                                                                                                 | VPS host, `docs/t3-devbox.md` mục 5                                                                                                                                                 |
| Đĩa emergency read-only → DỪNG mọi cài đặt/vá, đi thẳng reboot để fsck tự sửa                                           | Kernel đã khoá ghi toàn đĩa (EXT4_FLAGS_EMERGENCY_RO) — cài/vá gì cũng fail im lặng; đĩa read-only thì không mất thêm dữ liệu khi đợi; ext4 có cờ lỗi nên fsck tự quét sửa lúc boot — đã chữa thành công 25/09 không mất gì                                                                                                                                                                                      | Cố fsck tay online (đĩa đang mount, nguy hiểm); cài lại VPS (mất hạ tầng đã dựng); bỏ qua rồi cài tiếp (lỗi âm thầm kéo dài)                                                                                                                                           | Quy trình VPS, `docs/t3-devbox.md` mục 4                                                                                                                                            |
| Cổng tín hiệu cấu hình SUY hợp đồng từ code (`getBotConfig` ∩ schema guilds), không dùng danh sách field gõ tay         | Lớp bug này (thay đổi từ dashboard không tới bot trong 30 phút) đã tái diễn 7 lần ở 7 file khác nhau; danh sách gõ tay lệch ngay khi có field/mutation mới, còn hợp đồng suy từ code thì tự cập nhật. Cổng tự-dò còn tìm ra chỗ mà bản liệt kê ghi tay của phiên trước bỏ sót — `resetHeat` và `requestUnlock`, tức 2 nút bấm đứng im với người dùng                                                             | Rà tay từng file mutation (đã bỏ sót 6 chỗ); liệt kê field trong file cấu hình của script (lệch theo thời gian); chỉ vá mà không thêm cổng (lớp bug quay lại)                                                                                                          | `scripts/{check-settings-signal,test-settings-signal}.cjs`, `.github/workflows/ci.yml`, `AGENTS.md`, `.opencode/plugins/guardrails.js`                                              |
| Bot tự ghi cấu hình → xoá cache bằng PROXY tập trung trong `convex.js`, KHÔNG gọi `store.invalidate()` ở từng call site | Đo được 7/35 chỗ gọi quên invalidate, gây bug nặng: server bị khoá kênh lâu hơn cấu hình (auto-lock 5 phút thành tới 30 phút), mở khoá xong bot tưởng còn khoá nên BỎ QUA raid sau, báo cáo ngày gửi lặp, restore xong vẫn chạy cấu hình cũ. Proxy sẵn có (đang chèn/xoay botKey) là chỗ DUY NHẤT mọi lượt ghi đi qua → không thể quên; đồng thời gỡ luôn chặn "patch không tới được đuôi `backup.js` 2126 dòng" | Thêm `store.invalidate()` thủ công ở cả 35 call site (đã chứng minh không bền); hạ TTL cache xuống ngắn (bùng reads Convex free tier — lý do cache 30 phút tồn tại)                                                                                                    | `bot/src/convex.js`, `bot/src/{tick.js,lockdown.js}`, `bot/src/handlers/joinGate.js`, `scripts/{test-convex-client,test-tick}.cjs`                                                  |
| Cổng phân tích tĩnh phải BỎ COMMENT trước khi kết luận, và đòi đúng cú pháp GHI tín hiệu                                | Test hồi quy trên file thật phát hiện cổng bị đánh lừa: gỡ code `settingsChangedAt` nhưng comment giải thích "// settingsChangedAt: …" còn nguyên → cổng báo SẠCH. Một cổng có thể bị lừa bởi chính comment giải thích nó là cổng dở                                                                                                                                                                             | Chỉ dò chuỗi con `settingsChangedAt` trong thân hàm (đã bị lừa thật); đòi cú pháp ghi mà không bỏ comment (comment dạng `settingsChangedAt: …` vẫn khớp regex)                                                                                                         | `scripts/check-settings-signal.cjs`, `scripts/test-settings-signal.cjs`                                                                                                             |
| Webhook log dùng CHUNG tín hiệu `settingsChangedAt`, không thêm field tín hiệu riêng                                    | Cache webhook phía bot (`webhookHub`) có TTL 5 phút — cùng bản chất "dashboard sửa mà bot chưa biết", chỉ khác con số. Một tín hiệu duy nhất (tick xoá cả cache config lẫn cache webhook) giữ 1 đường ống, 1 chỗ kiểm; thêm field thứ hai là thêm chỗ để quên                                                                                                                                                    | Thêm `webhooksChangedAt` riêng (thêm field + thêm nhánh tick để lệch nhau); để nguyên 5 phút (giao diện hứa ~3 phút cho MỌI thay đổi)                                                                                                                                  | `convex/webhooks.ts`, `bot/src/tick.js`, `scripts/test-tick.cjs`                                                                                                                    |
| Thẻ ảnh chào: NHÚNG font Noto Sans (OFL, 569 KB) vào repo thay vì dùng font hệ thống                                    | Máy chủ build không có font nào (`GlobalFonts.families === 0`) → `fillText` im lặng không vẽ chữ, ảnh vẫn ra PNG hợp lệ nên KHÔNG ai biết là sai. Font có sẵn trong máy (KaTeX/CMU) thiếu toàn bộ dấu tiếng Việt. Font nhúng là cách duy nhất tất định cho sản phẩm tiếng Việt, và bot zip sẵn có nên không phải thêm bước deploy                                                                                | Dựa vào font hệ thống (im lặng mất chữ trên máy trần); dùng font có sẵn trong máy (mất dấu tiếng Việt); `apt install fonts-dejavu-core` trên VPS (thêm bước hạ tầng, không kiểm chứng được ở CI); sinh file font bằng base64 trong repo (khó bảo trì, cùng kích thước) | `bot/assets/fonts/`, `bot/src/handlers/welcomeCard.js`, `bot/package.json`                                                                                                          |
| Nhận diện glyph khuyết bằng CHỮ KÝ ĐIỂM ẢNH, không so bề rộng                                                           | Trong Noto Sans chữ "V" rộng đúng bằng glyph khuyết (14.4px) → so bề rộng xoá oan ký tự thật khỏi ảnh (bắt được qua test: "Nguyễn Văn A" mất chữ V). Điểm ảnh là tiêu chí duy nhất phân biệt được glyph thật với ô tofu                                                                                                                                                                                          | So bề rộng (đã sai thật); tin là font luôn có đủ glyph (emoji sẽ ra ô vuông)                                                                                                                                                                                           | `bot/src/handlers/welcomeCard.js`, `scripts/test-welcome-card.ts`                                                                                                                   |
| Bot báo khả năng vẽ thẻ qua mutation riêng `status:reportCardCapability`                                                | Bật thẻ mà máy chủ bot không có thư viện/font thì người dùng chỉ thấy "đã bật" mà không có ảnh nào — đúng lớp lỗi im lặng dự án đang chặn. Đi nhờ `botSyncGuilds` không được: vùng patch đó nằm quá sâu trong `convex/guilds.ts` (công cụ patch chỉ sửa được nửa đầu file 67 KB), và đây là thông tin toàn cục không thuộc vòng sync per-guild                                                                   | Đi nhờ `botSyncGuilds.globalStatus` (không patch được vùng cần sửa); cache kết quả trong panel (đoán, có thể sai); im lặng để người dùng tự đoán                                                                                                                       | `convex/status.ts`, `convex/schema.ts`, `convex/guilds.ts`, `bot/src/index.js`                                                                                                      |
| Ảnh welcome/goodbye lưu trong Convex storage (tải từ dashboard), KHÔNG thêm thư viện vẽ ảnh cho bot                     | Người dùng cần "chèn ảnh" và một bản xem trước thật; phần LỚN giá trị nằm ở chỗ tự tải ảnh lên được thay vì phải host ở đâu đó rồi dán URL. `ctx.storage` đã có sẵn (backup, hidden đang dùng) nên không thêm dependency, không rủi ro build/native. Bot tự vẽ PNG theo từng thành viên (kiểu thẻ chào) là việc RIÊNG, cần `@napi-rs/canvas` — tách ra đợt sau, đúng thứ tự "A trước, B sau" người dùng chọn     | Gộp luôn canvas vào lượt này (thêm dependency native vào bot production, rủi ro cao hơn giá trị bước đầu); chỉ cho dán URL ảnh (bắt người dùng tự host ảnh — đúng thứ họ đang muốn bỏ)                                                                                 | `convex/guilds.ts`, `convex/schema.ts`, `bot/src/handlers/guildSync.js`, `src/components/dashboard/{WelcomePanel,GreetingPreview}.tsx`                                              |
| Emoji tuỳ chỉnh đồng bộ vào bảng `guildEmojis` riêng, chỉ ghi khi danh sách đổi                                         | Panel cần danh sách emoji để dựng mã `<:ten:id>` chính xác (gõ tay sai một ký tự là Discord hiện chữ thô). Bảng riêng theo khuôn `guildChannels`/`guildRoles` đã có; so danh sách trước khi ghi để dashboard không re-render mỗi vòng sync 5 phút                                                                                                                                                                | Nhồi danh sách emoji vào document `guilds` (phình document, mọi lượt heartbeat đều kéo theo); để người dùng tự gõ mã emoji (đúng lớp lỗi cần chặn)                                                                                                                     | `convex/schema.ts`, `convex/guilds.ts`, `bot/src/handlers/guildSync.js`, `src/lib/types.ts`                                                                                         |
| Xem trước welcome/goodbye render TOKEN thành React node, không dùng `dangerouslySetInnerHTML`                           | Nội dung là người dùng nhập rồi bot gửi lên Discord — dựng HTML từ chuỗi đó trong dashboard là tự tạo lỗ XSS cho chính chủ server. Tokenizer tách ra thành phần rồi render bằng React nên text thuần được escape tự động, không có đường chèn HTML                                                                                                                                                               | Dùng `innerHTML` + thay `<:…>` bằng `<img>` (ngắn hơn nhưng mở XSS); không có preview (người dùng chỉ biết tin nhắn sai khi thành viên thật đã thấy)                                                                                                                   | `src/components/dashboard/GreetingPreview.tsx`, `scripts/test-greeting-preview.ts`                                                                                                  |
| Cấu hình dashboard tới bot bằng tín hiệu `settingsChangedAt` qua batch tick, KHÔNG giảm TTL cache                       | Cache `getConfig` là 30 phút (đã tối ưu có chủ đích để cắt reads cho Convex free tier) nhưng giao diện hứa "~3 phút" → thay đổi từ web tới bot chậm tới 30 phút, người dùng tưởng tính năng hỏng (bug thật 23/09 với welcome/goodbye). Tín hiệu đi nhờ batch `bot_tick:getPendingJobs` đã có sẵn (0 query thêm, bot xoá cache đúng guild 1 lần/thay đổi) nên không đánh đổi gì về hiệu năng                      | Giảm `CONFIG_TTL_MS` toàn cục (bùng số reads mỗi tin nhắn ở mọi server); dùng `updatedAt` làm tín hiệu (chính bot bump mỗi lượt sync → mất cache vĩnh viễn); thêm query "cấu hình đổi chưa" mỗi guild (tốn function call đúng thứ đang tiết kiệm)                      | `convex/schema.ts`, `convex/{guilds,antinuke,altDetection,autoreplies,backup,bot_tick}.ts`, `bot/src/tick.js`, `scripts/test-tick.cjs`, `scripts/test-bot-tick-settings.ts`         |
| Web biết kết quả backup qua mốc `backupFinishedAt` + cờ `backupUnchanged`, không suy đoán                               | Trước đây bot xoá cờ `backupRequested` là cách duy nhất báo "xong" → web không phân biệt được "đã tạo bản mới" với "bỏ qua vì server không đổi" với "lưu Convex thất bại", nên bấm "Backup ngay" xong không có bản nào cũng không có thông báo (bug thật 23/09). Hai field nói đúng sự việc đã xảy ra, giữ được cả trường hợp skip và trường hợp lỗi                                                             | Đoán theo `updatedAt`/so số lượng bản backup (mơ hồ khi backup trùng giây hoặc tự dọn chỉ giữ 3 bản); chỉ sửa copy trên web (không sửa gốc: web vẫn mù kết quả thật)                                                                                                   | `convex/schema.ts`, `convex/{bot_writes,backup}.ts`, `bot/src/handlers/backup.js`, `src/components/dashboard/BackupPanel.tsx`                                                       |
| Trang pháp lý dùng CHUNG 1 component + nội dung 3 thứ tiếng ở `legalContent.ts`                                         | Discord chỉ xác minh bot khi ToS + Privacy có URL RIÊNG, công khai, không cần đăng nhập — dùng luôn tên miền dashboard (3 `<Route>`, cùng một `LegalPage` khác `slug`) nên không phải nuôi site phụ; văn bản dài 3 thứ tiếng để trong module nội dung tự chứa thay vì từ điển key-VI (nhét vào `i18n.en/de.ts` thì mỗi câu phải sửa 3 file và không ai đối chiếu nổi)                                            | Dựng site pháp lý riêng (thêm hạ tầng + tên miền để xác minh); nhét ~150 đoạn văn vào từ điển (không đọc/đối chiếu được, dễ lệch bản dịch)                                                                                                                             | `src/lib/legalContent.ts`, `src/pages/LegalPage.tsx`, `src/App.tsx`, `src/components/landing/Footer.tsx`, `scripts/test-web-contracts.cjs`                                          |
| Cổng i18n #3f: miễn luật nhãn dữ liệu cho file `@i18n-content`, ĐỔI LẠI kiểm cấu trúc vi/en/de                          | Miễn trừ là lỗ nếu không kiểm gì; ở đây cổng so CÂY đường dẫn `vi` với `en`/`de`: thiếu nhánh, ô rỗng, hoặc giữ nguyên tiếng Việt đều là FAIL cứng → vừa có chỗ cho văn bản dài, vừa không có đường lọt chuỗi chưa dịch                                                                                                                                                                                          | Bỏ marker rồi thêm ngoại lệ theo tên file (không ai kiểm nội dung); để văn bản ngoài `src/` (không có cổng nào phủ)                                                                                                                                                    | `scripts/check-i18n.cjs`, `scripts/test-i18n.cjs`, `src/lib/legalContent.ts`                                                                                                        |
| Bỏ tên model/hãng và tên bot đối thủ khỏi chuỗi UI                                                                      | Chuỗi chào hàng ghi "AI Mimu v2.5" (tên model nội bộ, dễ lỗi thời) và 4 chỗ gọi "kiểu Carl-bot": vừa hạ uy tín sản phẩm, vừa nhập nhằng nguồn gốc, vừa sai khi mô hình/thị trường đổi → mô tả bằng chính tính năng, có test chặn (Carl-bot/MEE6/Dyno/ProBot trong `translate("…")`)                                                                                                                              | Giữ nguyên tên model cho "cụ thể"; chỉ đổi 1-2 chỗ nhìn thấy (còn 2 chỗ trong panel)                                                                                                                                                                                   | `src/components/landing/sections.tsx`, `src/components/dashboard/{ModerationPanel,ModActionsPanel,SettingsPanel}.tsx`, `src/lib/i18n.{en,de}*.ts`, `scripts/test-web-contracts.cjs` |
| Cổng i18n #3e bắt mảng VI render `{m}` trong `.map()`                                                                   | Mảng khai báo ngoài JSX nên cổng JsxText và cổng `{x.label}` đều mù — 20 nhãn module trang chủ đã lọt cho người dùng EN/DE. Cổng AST bắt đúng lớp lỗi này, chỉ nhận mảng VI khai báo trong cùng file nên không báo nhầm mảng ngoài                                                                                                                                                                               | Cổng regex theo dòng (đã bỏ sót chính lỗi này, lại báo nhầm 665 dòng vì comment/translate nhiều dòng)                                                                                                                                                                  | scripts/check-i18n.cjs, scripts/test-i18n.cjs, src/components/landing/sections.tsx                                                                                                  |
| Cổng i18n báo MỀM bản DE mồ côi, không cứng                                                                             | Bản DE không có bản EN là rác không bao giờ hiển thị (tra cứu theo chuỗi VI, thiếu EN thì rơi về VI) — nợ vệ sinh từ điển, không đáng để chặn ship; dọn dần theo lô và theo dõi bằng mục ℹ️ của guard                                                                                                                                                                                                            | FAIL cứng: CI đỏ vì vài entry rác!                                                                                                                                                                                                                                     | scripts/check-i18n.cjs, scripts/test-i18n.cjs                                                                                                                                       |
| Bot AI mặc định model sống + tự vá 400/404 (mirror haimiya.ts)                                                          | Kiểm tra live 20/09/2026: Groq `llama-3.3-70b-versatile` đã retire, Kira không còn `mimo-v2.5-free` (chỉ còn `mimo-v2.5`) → bot gọi model chết, đốt cả fallback chain vô ích; self-heal thử lại 1 lần với `openai/gpt-oss-120b`                                                                                                                                                                                  | Giữ default cũ (mọi call Groq/Kira lỗi); hardcode model mới không self-heal (chết tiếp khi retire)                                                                                                                                                                     | `bot/src/ai.js`, `scripts/test-ai-fallback.cjs`, `bot/README.md`, `AGENTS.md`                                                                                                       |
| Prompt AI few-shot + benign checklist + hiệu chuẩn confidence                                                           | Model nhỏ/free hay đoán mò và ban oan; ví dụ raid/benign + checklist dương tính giả + quy tắc "≥0.8 chỉ khi ≥2 tín hiệu" rẻ hơn đổi model lớn                                                                                                                                                                                                                                                                    | Giữ prompt mô tả chung (độ chính xác thấp, đã thấy ở test-chat-flow)                                                                                                                                                                                                   | `bot/src/ai.js`, `scripts/test-{ai-fallback,chat-flow-classify}.cjs`                                                                                                                |
| AI đối chiếu mẫu scam bot tự học (knownThreats, 0 token)                                                                | raidSamples → threat intel đã có sẵn nhưng AI đoán chay không dùng → đưa keywords/phrases đã xác nhận vào prompt classify                                                                                                                                                                                                                                                                                        | Để AI đoán chay (bỏ phí dữ liệu raid thật); hardcode mẫu vào prompt (lỗi thời nhanh)                                                                                                                                                                                   | `bot/src/{ai.js,handlers/filters.js,handlers/antinuke/{ai,messages}.js}`, `scripts/test-{ai-fallback,antinuke-ai,misfire-guard}.cjs`                                                |
| Eval AI live bằng script tay (.mjs), không vào suite CI                                                                 | Cần số liệu chính xác/trễ khi đổi prompt nhưng live test cần key thật, không hermetic được                                                                                                                                                                                                                                                                                                                       | Nhét vào run-all-tests (fail CI khi không key/mạng); không đo gì (mù)                                                                                                                                                                                                  | `scripts/test-ai-raid-eval.mjs`                                                                                                                                                     |
| massJoin: 1 làn sóng = 1 xử lý + gate phối hợp + AI phủ quyết                                                           | Mỗi join vượt ngưỡng chạy lại toàn bộ pipeline gây log trùng/phạt lặp/lockdown lặp; acc mới đơn lẻ (điểm 2) không phải bằng chứng raid nên cụm cần ≥1 tín hiệu phối hợp cứng (tên máy/avatar trùng/default ồ ạt) hoặc ≥2 mềm + AI veto trước phạt                                                                                                                                                                | Giữ multi-fire (spam log, test cũ ghi nhận hành vi bug); chỉ dùng ratio≥0.5 (báo raid oan sóng bạn bè acc mới)                                                                                                                                                         | `bot/src/handlers/antinuke/{shared,members}.js`, `scripts/test-{false-positive,member-layers}.cjs`                                                                                  |
| Gate phạt cá nhân massJoin nâng 3→4 (đủ 2 tín hiệu độc lập)                                                             | Ngưỡng 3 kick oan người thật mới lập acc (mới + default avatar) đi kèm sóng đông; raid tool luôn dính ≥2 tín hiệu nên không lọt                                                                                                                                                                                                                                                                                  | Giữ ngưỡng 3 (ban oan); nâng lên 5 (lọt raid chỉ có 2 tín hiệu)                                                                                                                                                                                                        | `bot/src/handlers/antinuke/members.js`, `scripts/test-member-layers.cjs`                                                                                                            |
| Log chỉ qua webhook nằm đúng kênh đích, ngược lại gửi thẳng kênh                                                        | Case ban/kick đi qua webhook mặc định ở kênh log chung thay vì kênh hình phạt đã cấu hình (sai kênh, đã chứng minh bằng test RED trên code cũ)                                                                                                                                                                                                                                                                   | Giữ match theo eventType bất kể kênh (sai kênh); tạo webhook mới mỗi lần (spam webhook)                                                                                                                                                                                | `bot/src/util.js`, `bot/src/webhookHub.js`, `scripts/test-webhook-hub.cjs`                                                                                                          |
| `inferEventType` phân biệt raid thật vs nuke cấu trúc                                                                   | Chữ "Raid" trong "Nuke/Raid" khiến mọi log antinuke bị gắn nhãn raid, webhook lọc riêng "antinuke" không bao giờ nhận log; "Alt Detection" không khớp regex "alt detected" cũ                                                                                                                                                                                                                                    | Giữ regex cũ (sai nhãn); truyền eventType tay ở từng điểm gọi (30+ chỗ, dễ sót)                                                                                                                                                                                        | `bot/src/util.js`                                                                                                                                                                   |
| Xoá `AnalyticsPanel` + `AuditLogPanel` thay vì nối lại vào UI                                                           | Cả hai không được import ở đâu (dead code) sau redesign; lịch sử thật đã do `GuildHistory` (`api.reports.historyForGuild`) phục vụ — giữ lại gây hiểu nhầm còn dùng được                                                                                                                                                                                                                                         | Nối lại vào dashboard (thêm chỗ + trùng chức năng `GuildHistory`); để nguyên (nợ kỹ thuật, lint không bắt)                                                                                                                                                             | `src/components/dashboard/`, `scripts/test-web-contracts.cjs`                                                                                                                       |
| Dùng bảng xám mono cho SERVER_THEMES (5 sắc độ)                                                                         | UI chuẩn Vercel đen trắng; key cũ trong DB vẫn map xám → không cần migration                                                                                                                                                                                                                                                                                                                                     | Giữ 8 theme màu (phá nhất quán redesign); xoá cột theme (vỡ dữ liệu cũ)                                                                                                                                                                                                | `src/lib/constants.ts`, `GuildPage`, `SettingsPanel`                                                                                                                                |
| Giữ đỏ `--danger` cho mức heat cao nhất                                                                                 | Đỏ dành riêng cho trạng thái phạt — là ngữ nghĩa, không phải trang trí                                                                                                                                                                                                                                                                                                                                           | Grayscale tuyệt đối (mất tín hiệu nguy hiểm)                                                                                                                                                                                                                           | `AnalyticsPanel`, tokens                                                                                                                                                            |
| Taskbar dạng pill nhỏ thay thanh dọc full-height                                                                        | Full-height lấn màn hình, glow/emoji làm UI rẻ tiền                                                                                                                                                                                                                                                                                                                                                              | Giữ thanh dọc cũ (chiếm chỗ, không Escape/close-outside)                                                                                                                                                                                                               | `src/components/Taskbar.tsx`                                                                                                                                                        |
| Convex deploy 2 đường (CI + VPS guardrail)                                                                              | CI đảm bảo luôn có bản mới; VPS deploy nhanh khi cần — deploy idempotent nên song song vô hại                                                                                                                                                                                                                                                                                                                    | Chỉ CI (chậm khi cần fix gấp); chỉ VPS (phụ thuộc key 1 chỗ)                                                                                                                                                                                                           | CI workflow, `opencode.json` guardrail                                                                                                                                              |
| Provider AI chính là Kiira qua retry-proxy local                                                                        | Proxy retry/tự chữa + 43 model; Groq retire model cũ nên fallback có sẵn self-heal                                                                                                                                                                                                                                                                                                                               | Groq trực tiếp (model dễ retire); Cline làm chính (đốt credit, không phải free)                                                                                                                                                                                        | `bot/src/ai.js`, `convex/haimiya.ts`, `scripts/kiira-retry-proxy.mjs`                                                                                                               |
| Giữ `vite.config.ts` `hmr: false`                                                                                       | Yêu cầu của Freebuff platform — không xoá                                                                                                                                                                                                                                                                                                                                                                        | Bật HMR dev (platform bị lỗi sync)                                                                                                                                                                                                                                     | `vite.config.ts`                                                                                                                                                                    |
| botKey = SHA-256("protogon-bot-key::" + seed)                                                                           | Secret không nằm trong repo, bot tự bootstrap + cache `.bot-key`                                                                                                                                                                                                                                                                                                                                                 | Key hardcode/env repo (rò rỉ); backdoor nào đó (tuyệt đối không)                                                                                                                                                                                                       | `convex/botAuth.ts`                                                                                                                                                                 |
| node_modules hỏng → clean install trước khi tìm bug code                                                                | Case thật 09/2026: build gãy vì cây file không toàn vẹn sau upgrade, fix bằng `rm -rf node_modules && bun install --frozen-lockfile`                                                                                                                                                                                                                                                                             | Vá từng file trong node_modules (không bền); đổ lỗi code (mất thời gian)                                                                                                                                                                                               | Quy trình debug VPS/dashboard                                                                                                                                                       |
