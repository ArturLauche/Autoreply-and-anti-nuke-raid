node_modules
dist
isolate/
.env
.env.local
.env.*.local
.bot-key
bot/node_modules
bot/.env
bot/.bot-key
bot/*.zip
.convex
*.tsbuildinfo
*.log
.DS_Store

# Coverage report (sinh bởi c8 — chỉ xem local/CI artifact)
coverage/

# Mock discord.js sinh tạm khi chạy test suites
bot/test-djs-mock.cjs

# Snapshot backup cục bộ của bot (C1 — dữ liệu runtime trên VPS, không vào git)
bot/data/

# Mock tạm sinh khi chạy test-boost-modules (đã dời sang tmpdir nhưng đề phòng)
scripts/_djs-mock-boost.cjs

# Artifacts audit bảo mật (skill security-audit — chỉ xem local, không vào git)
.security-audit/
